import React from 'react';

const LoadingSpinner = () => {
    return (
        <div className="inline-block w-16 h-16 border-4 rounded-full spinner-border animate-spin border-t-indigo-600"></div>
    );
};

export default LoadingSpinner;
